<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Only the two permissions the app actually uses are declared.
      SYSTEM_ALERT_WINDOW ("draw over other apps") and POST_NOTIFICATIONS were
      removed: nothing in the code used them, and declaring unused sensitive
      permissions makes Play Protect flag the APK as "accessing sensitive data".
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestureVolume">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".VolumeGestureService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".VolumeWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.skywave.gesturevolume.ACTION_VOL_UP" />
                <action android:name="com.skywave.gesturevolume.ACTION_VOL_DOWN" />
                <action android:name="com.skywave.gesturevolume.ACTION_MUTE_TOGGLE" />
                <action android:name="com.skywave.gesturevolume.ACTION_SET_LEVEL" />
                <action android:name="android.media.VOLUME_CHANGED_ACTION" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/volume_widget_info" />
        </receiver>

    </application>
</manifest>
